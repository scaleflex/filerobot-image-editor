import styled from 'styled-components';

const StyledBottombar = styled.div`
  display: flex;
  align-items: center;
  width: fit-content;
  flex-wrap: wrap;
  margin: 0 auto;
`;

export {
  StyledBottombar,
}
