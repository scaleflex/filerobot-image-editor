import { lazy } from 'react';

const Shapes = lazy(() => import('./Shapes'));

// const Text = lazy(() => import('./Text'));

// const Image = lazy(() => import('./Image'));

// const Line = lazy(() => import('./Line'));

// const Arrow = lazy(() => import('./Arrow'));

// const Freehand = lazy(() => import('./Freehand'));

export {
  Shapes,
  // Text,
  // Image,
  // Line,
  // Arrow,
  // Freehand,
};
