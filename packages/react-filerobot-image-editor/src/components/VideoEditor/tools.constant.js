import { Trim } from 'components/VideoEditor/Tools/Trim';
import { TOOLS_IDS } from 'utils/constants';

export const VIDEO_TOOLS_ITEMS = {
  [TOOLS_IDS.TRIM]: {
    id: TOOLS_IDS.TRIM,
    Item: Trim,
  },
};
